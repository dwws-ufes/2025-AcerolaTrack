vaadin:
  launch-browser: true
  url-mapping: /front/*
  exclude-urls: "/v1/*"
server:
  port: 8081

spring:
  application:
    name: "acerolatrack"
  datasource:
    url: "jdbc:postgresql://localhost:5432/postgres"
    username: "postgres"
    password: "example"
